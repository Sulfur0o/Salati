<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_glance_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/salati_widget_background"
    android:gravity="center_vertical"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <!-- Left Column: Context & Next Prayer Name -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_brand_name"
                android:textColor="@color/salati_widget_accent"
                android:textStyle="bold"
                android:textSize="11sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_dot"
                android:textColor="@color/salati_widget_on_surface_variant"
                android:textSize="10.5sp" />

            <TextView
                android:id="@+id/widget_glance_location"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_location_placeholder"
                android:textColor="@color/salati_widget_on_surface_variant"
                android:textSize="10.5sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_glance_prayer_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="@color/salati_widget_on_surface"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Right Column: Large Prayer Time -->
    <TextView
        android:id="@+id/widget_glance_prayer_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_placeholder_time"
        android:textColor="@color/salati_widget_accent"
        android:textSize="22sp"
        android:textStyle="bold" />

</LinearLayout>
